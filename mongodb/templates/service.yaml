---
apiVersion: v1 
kind: Service 
metadata:
  name: mongodb
  namespace: roboshop
spec:
  selector:
      project: roboshop 
      tier: db
      component: mongodb
  ports:
    - protocol: TCP 
      port: 27017  # Service Port
      targetPort: 27017 # Container Port